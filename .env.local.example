# Copy this file to .env.local and paste your MongoDB Atlas connection string.
# Replace USERNAME and PASSWORD with your database user.
# Never commit .env.local to Git (it is already in .gitignore).

MONGODB_URI="mongodb+srv://USERNAME:PASSWORD@cluster0.xxxxx.mongodb.net/dhandifan?retryWrites=true&w=majority"
